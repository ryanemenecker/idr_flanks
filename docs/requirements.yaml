name: docs_idr_flanks
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton




    # Pip-only installs
  - pip:
    - -e ../

